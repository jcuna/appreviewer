
<%= simple_form_for(@status) do |f| %>
<!--  <% if @status.errors.any? %> 
    <div id="error_explanation">
      <h2><%= pluralize(@status.errors.count, "error") %> prohibited this status from being saved:
      </h2>
      <ul>
      <% @status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
   <% end %> -->

  <div class="row">
  <div class="col-md-3">
      <%= f.input :application, label: false, placeholder: "App Name", input_html: { class: "form-control"} %>
  </div>
</div><br>
  
    <%= f.input :content, label: false, placeholder: "Review Content", input_html: { class: "form-control", rows: "4"} %>


<br>

    <%= f.submit "Post Review", :class => "btn btn-lg btn btn-success btn-block" %>

<% end %>
