
<%= simple_form_for(@status, html: {class: "well"}) do |f| %>
  <% if @status.errors.any? %> 
    <div id="error_explanation">
      <h2><%= pluralize(@status.errors.count, "error") %> prohibited this status from being saved:</h2>

      <ul>
      <% @status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.input :user_id, collection: User.all, label_method: :full_name %><br>
    <%= f.input :content, :input_html => {style: "height:100px; width:500px"} %>


<br>
  <div class="btn btn-default btn-lg btn-block">
    <%= f.button :submit, "Post Review" %>
  </div>
<% end %>
